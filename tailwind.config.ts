// this file is not being used
// i have left it to document that this was the initial tailwind config file
// after install of shadcdn, we are using tailwind.config.js

import type { Config } from 'tailwindcss'

export default {
  content: ["./app/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config

